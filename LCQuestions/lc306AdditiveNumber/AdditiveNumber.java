package LCQuestions.lc306AdditiveNumber;

public class AdditiveNumber {
    /**
     * 第一个数范围（n - 1）/2
     * 第二个数范围q: n - j >= i && n - j >= j - i
     * @param num
     * @return
     */
    public boolean isAdditiveNumber(String num) {
        return true;
    }

}
